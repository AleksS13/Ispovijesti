<%- include('../partials/layout', {
  title: title,
  body: include('../partials/admin_confessions_body', {
    confessions,
    activeStatus,
    page,
    limit,
    total,
    totalPages
  })
}) %>
